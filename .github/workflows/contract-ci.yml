name: Contract CI

on:
  push:
    branches: [main, master]
    paths:
      - "contracts/**"
      - "Contract_archived/**"
  pull_request:
    branches: [main, master]
    paths:
      - "contracts/**"
      - "Contract_archived/**"

jobs:
  build-and-test:
    name: Build and Test Contracts
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: contracts/earn-quest
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Add wasm target
        run: rustup target add wasm32-unknown-unknown

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('contracts/earn-quest/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache target directory
        uses: actions/cache@v4
        with:
          path: contracts/earn-quest/target
          key: ${{ runner.os }}-cargo-target-${{ hashFiles('contracts/earn-quest/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-target-

      - name: Build workspace
        run: cargo build --release

      - name: Run tests
        run: cargo test

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy lint
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: License check
        run: |
          cargo install cargo-deny || true
          make license-check

      - name: Build WASM
        run: cargo build --release --target wasm32-unknown-unknown

      - name: Write matrix status file
        if: always()
        run: |
          echo "os=${{ matrix.os }}" > ci-status-${{ matrix.os }}.txt
          echo "job_status=${{ job.status }}" >> ci-status-${{ matrix.os }}.txt
          echo "runner=${{ runner.os }}" >> ci-status-${{ matrix.os }}.txt

      - name: Upload matrix status artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-matrix-status-${{ matrix.os }}
          path: ci-status-${{ matrix.os }}.txt

  contract-ci-gate:
    name: Contract CI Gate
    runs-on: ubuntu-latest
    needs: [build-and-test]
    if: always()
    steps:
      - name: Download all matrix status artifacts
        if: always()
        uses: actions/download-artifact@v4
        with:
          path: ./ci-artifacts

      - name: Print matrix statuses
        if: always()
        run: |
          echo "Listing artifacts:"
          ls -la ci-artifacts || true
          echo "--- Matrix status files ---"
          find ci-artifacts -type f -name 'ci-status-*.txt' -print -exec sed -n '1,200p' {} \; || true

      - name: Validate results
        run: |
          build_status="${{ needs.build-and-test.result }}"
          echo "Build status: $build_status"
          if [ "$build_status" != "success" ]; then
            echo "Contract CI failed"
            exit 1
          fi
          echo "All contract CI checks passed"